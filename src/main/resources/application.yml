spring:
  datasource:
    url: jdbc:mysql://117.72.112.62:3306/testdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 200522
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: json-test
  data:
    redis:
      port: 6379
      host: localhost
server:
  port: 8080
  address: 0.0.0.0
logging:
  level:
    com.example.zzk: info
    org.springframework.web: info
    org.mybatis: info
    org.apache.ibatis: info
# MyBatis Plus配置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true