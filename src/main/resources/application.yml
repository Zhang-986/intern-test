spring:
  datasource:
    url: jdbc:mysql://82.157.203.231:3306/testdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 200522
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: json-test-${SERVER_PORT:8080}  # 实例名称包含端口号
  data:
    redis:
      port: 6379
      host: 82.157.203.231
      password: redis-stack
server:
  port: ${SERVER_PORT:8080}  # 支持通过环境变量指定端口
  address: 0.0.0.0
logging:
  level:
    com.example.zzk: info
    org.springframework.web: info
    org.mybatis: info
    org.apache.ibatis: info
# MyBatis Plus配置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  cofiguration:
    map-underscore-to-camel-case: true